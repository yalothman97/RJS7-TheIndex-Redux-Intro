const initialState = {
  authors: "COMPLETE ME",
  newAuthorId: "DO YOU NEED ME??"
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
